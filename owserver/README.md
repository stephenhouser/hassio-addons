HASSIO Addon to expose OneWire devices connected via USB-Serial device on `ttyUSB1`.

Devices can be read/written via [OneWire Server Protocol](http://owfs.org/index.php?page=owserver-protocol) over TCP/IP.

